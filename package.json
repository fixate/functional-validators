{
  "name": "functional-validators",
  "version": "0.0.1",
  "description": "Composable, pure validation functions.",
  "main": "./lib/index.js",
  "scripts": {
    "compile": "$(npm bin)/babel --presets es2015,stage-0 -d lib/ src/",
    "prepublish": "npm run compile",
    "watch": "when-changed -r -1 -s ./src npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fixate/functional-validators.git"
  },
  "keywords": [
    "validation",
    "functional",
    "validate"
  ],
  "author": "Stan Bondi <stan@fixate.it>",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/fixate/functional-validators/issues"
  },
  "homepage": "https://github.com/fixate/functional-validators#readme",
  "dependencies": {
    "lodash": "^4.13.1"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0",
    "tape": "^4.6.0"
  }
}
